{
  "name": "@chain/cli",
  "version": "0.0.1",
  "description": "chain init — scaffolds a new Chain-consuming app, the way `npm create tauri-app` or `expo init` do.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.18.0"
  },
  "bin": {
    "chain": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
